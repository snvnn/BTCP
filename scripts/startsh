cd ~/BTCP
source venv/bin/activate         # 반드시 실행
which python                     # → ~/BTCP/venv/bin/python 여야 함
which uvicorn                    # → ~/BTCP/venv/bin/uvicorn 여야 함

# 이 상태에서 실행
uvicorn predictor.api_server:app --reload